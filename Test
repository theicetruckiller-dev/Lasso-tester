local Players = game:GetService("Players")
local ReplicatedStorage = game:GetService("ReplicatedStorage")
local UserInputService = game:GetService("UserInputService")
local LocalPlayer = Players.LocalPlayer

local currentPenLocation = nil
_G.AutoBuyTreats = false
_G.WalkSpeed = 16
_G.AutoCatchEnabled = false
_G.TargetAnimalName = ""

local ScreenGui = Instance.new("ScreenGui")
ScreenGui.Name = "CustomLassoGui_FinalClean"
ScreenGui.Parent = LocalPlayer:WaitForChild("PlayerGui")
ScreenGui.ResetOnSpawn = false

local MainFrame = Instance.new("Frame")
MainFrame.Name = "MainFrame"
MainFrame.Size = UDim2.new(0, 280, 0, 280)
MainFrame.Position = UDim2.new(0.5, -140, 0.5, -140)
MainFrame.BackgroundColor3 = Color3.fromRGB(35, 35, 45)
MainFrame.BorderSizePixel = 1
MainFrame.BorderColor3 = Color3.fromRGB(80, 80, 90)
MainFrame.Active = true
MainFrame.Draggable = true
MainFrame.Parent = ScreenGui

local UICorner_Main = Instance.new("UICorner"); UICorner_Main.CornerRadius = UDim.new(0, 8); UICorner_Main.Parent = MainFrame
local TitleBar = Instance.new("Frame"); TitleBar.Name = "TitleBar"; TitleBar.Size = UDim2.new(1, 0, 0, 30); TitleBar.BackgroundColor3 = Color3.fromRGB(25, 25, 35); TitleBar.BorderSizePixel = 0; TitleBar.Parent = MainFrame
local UICorner_Title = Instance.new("UICorner"); UICorner_Title.CornerRadius = UDim.new(0, 8); UICorner_Title.Parent = TitleBar
local TitleLabel = Instance.new("TextLabel"); TitleLabel.Name = "TitleLabel"; TitleLabel.Size = UDim2.new(1, 0, 1, 0); TitleLabel.Text = "Lasso Animals Helper"; TitleLabel.Font = Enum.Font.SourceSansBold; TitleLabel.TextSize = 16; TitleLabel.TextColor3 = Color3.fromRGB(255, 255, 255); TitleLabel.BackgroundTransparency = 1; TitleLabel.Parent = TitleBar
local AutoBuyButton = Instance.new("TextButton"); AutoBuyButton.Name = "AutoBuyButton"; AutoBuyButton.Size = UDim2.new(0, 240, 0, 35); AutoBuyButton.Position = UDim2.new(0.5, -120, 0, 45); AutoBuyButton.BackgroundColor3 = Color3.fromRGB(200, 50, 50); AutoBuyButton.Font = Enum.Font.SourceSansBold; AutoBuyButton.Text = "Auto Buy: OFF"; AutoBuyButton.TextColor3 = Color3.fromRGB(255, 255, 255); AutoBuyButton.TextSize = 16; AutoBuyButton.Parent = MainFrame
local UICorner_AutoBuy = Instance.new("UICorner"); UICorner_AutoBuy.CornerRadius = UDim.new(0, 6); UICorner_AutoBuy.Parent = AutoBuyButton
local AutoCatchButton = Instance.new("TextButton"); AutoCatchButton.Name = "AutoCatchButton"; AutoCatchButton.Size = UDim2.new(0, 240, 0, 35); AutoCatchButton.Position = UDim2.new(0.5, -120, 0, 90); AutoCatchButton.BackgroundColor3 = Color3.fromRGB(200, 50, 50); AutoCatchButton.Font = Enum.Font.SourceSansBold; AutoCatchButton.Text = "Auto Catch: OFF"; AutoCatchButton.TextColor3 = Color3.fromRGB(255, 255, 255); AutoCatchButton.TextSize = 16; AutoCatchButton.Parent = MainFrame
local UICorner_AutoCatch = Instance.new("UICorner"); UICorner_AutoCatch.CornerRadius = UDim.new(0, 6); UICorner_AutoCatch.Parent = AutoCatchButton
local TargetAnimalInput = Instance.new("TextBox"); TargetAnimalInput.Name = "TargetAnimalInput"; TargetAnimalInput.Size = UDim2.new(0, 240, 0, 30); TargetAnimalInput.Position = UDim2.new(0.5, -120, 0, 135); TargetAnimalInput.BackgroundColor3 = Color3.fromRGB(20, 20, 20); TargetAnimalInput.TextColor3 = Color3.fromRGB(220, 220, 220); TargetAnimalInput.PlaceholderText = "Target Animal Name..."; TargetAnimalInput.Font = Enum.Font.SourceSans; TargetAnimalInput.TextSize = 14; TargetAnimalInput.Parent = MainFrame
local UICorner_Input = Instance.new("UICorner"); UICorner_Input.CornerRadius = UDim.new(0, 6); UICorner_Input.Parent = TargetAnimalInput
local SpeedSliderLabel = Instance.new("TextLabel"); SpeedSliderLabel.Name = "SpeedSliderLabel"; SpeedSliderLabel.Size = UDim2.new(1, -20, 0, 20); SpeedSliderLabel.Position = UDim2.new(0, 10, 0, 175); SpeedSliderLabel.Text = "Speed: 16"; SpeedSliderLabel.Font = Enum.Font.SourceSans; SpeedSliderLabel.TextSize = 14; SpeedSliderLabel.TextColor3 = Color3.fromRGB(220, 220, 220); SpeedSliderLabel.BackgroundTransparency = 1; SpeedSliderLabel.TextXAlignment = Enum.TextXAlignment.Left; SpeedSliderLabel.Parent = MainFrame
local SliderBackground = Instance.new("Frame"); SliderBackground.Name = "SliderBackground"; SliderBackground.Size = UDim2.new(0, 240, 0, 10); SliderBackground.Position = UDim2.new(0.5, -120, 0, 195); SliderBackground.BackgroundColor3 = Color3.fromRGB(20, 20, 20); SliderBackground.Parent = MainFrame
local UICorner_SliderBg = Instance.new("UICorner"); UICorner_SliderBg.Parent = SliderBackground
local SliderHandle = Instance.new("TextButton"); SliderHandle.Name = "SliderHandle"; SliderHandle.Size = UDim2.new(0, 20, 0, 20); SliderHandle.Position = UDim2.new(0, -10, 0.5, -10); SliderHandle.BackgroundColor3 = Color3.fromRGB(150, 150, 150); SliderHandle.BorderSizePixel = 0; SliderHandle.Draggable = false; SliderHandle.Text = ""; SliderHandle.Parent = SliderBackground
local UICorner_Handle = Instance.new("UICorner"); UICorner_Handle.Parent = SliderHandle
local MediumSpeedButton = Instance.new("TextButton"); MediumSpeedButton.Name = "MediumSpeedButton"; MediumSpeedButton.Size = UDim2.new(0, 75, 0, 30); MediumSpeedButton.Position = UDim2.new(0.5, -120, 0, 220); MediumSpeedButton.BackgroundColor3 = Color3.fromRGB(80, 80, 90); MediumSpeedButton.Font = Enum.Font.SourceSans; MediumSpeedButton.Text = "Medium Speed"; MediumSpeedButton.TextColor3 = Color3.fromRGB(255, 255, 255); MediumSpeedButton.TextSize = 14; MediumSpeedButton.Parent = MainFrame
local UICorner_Med = Instance.new("UICorner"); UICorner_Med.CornerRadius = UDim.new(0, 6); UICorner_Med.Parent = MediumSpeedButton
local FastSpeedButton = Instance.new("TextButton"); FastSpeedButton.Name = "FastSpeedButton"; FastSpeedButton.Size = UDim2.new(0, 75, 0, 30); FastSpeedButton.Position = UDim2.new(0.5, -37.5, 0, 220); FastSpeedButton.BackgroundColor3 = Color3.fromRGB(80, 80, 90); FastSpeedButton.Font = Enum.Font.SourceSans; FastSpeedButton.Text = "Fast Speed"; FastSpeedButton.TextColor3 = Color3.fromRGB(255, 255, 255); FastSpeedButton.TextSize = 14; FastSpeedButton.Parent = MainFrame
local UICorner_Fast = Instance.new("UICorner"); UICorner_Fast.CornerRadius = UDim.new(0, 6); UICorner_Fast.Parent = FastSpeedButton
local ResetSpeedButton = Instance.new("TextButton"); ResetSpeedButton.Name = "ResetSpeedButton"; ResetSpeedButton.Size = UDim2.new(0, 75, 0, 30); ResetSpeedButton.Position = UDim2.new(0.5, 45, 0, 220); ResetSpeedButton.BackgroundColor3 = Color3.fromRGB(80, 80, 90); ResetSpeedButton.Font = Enum.Font.SourceSans; ResetSpeedButton.Text = "Reset"; ResetSpeedButton.TextColor3 = Color3.fromRGB(255, 255, 255); ResetSpeedButton.TextSize = 14; ResetSpeedButton.Parent = MainFrame
local UICorner_Reset = Instance.new("UICorner"); UICorner_Reset.CornerRadius = UDim.new(0, 6); UICorner_Reset.Parent = ResetSpeedButton
local DiscordLabel = Instance.new("TextLabel"); DiscordLabel.Name = "DiscordLabel"; DiscordLabel.Size = UDim2.new(0, 200, 0, 15); DiscordLabel.Position = UDim2.new(0, 10, 1, -25); DiscordLabel.Text = "Discord - 847q"; DiscordLabel.Font = Enum.Font.SourceSans; DiscordLabel.TextSize = 14; DiscordLabel.TextColor3 = Color3.fromRGB(180, 180, 180); DiscordLabel.BackgroundTransparency = 1; DiscordLabel.TextXAlignment = Enum.TextXAlignment.Left; DiscordLabel.Parent = MainFrame
local ToggleVisibilityButton = Instance.new("TextButton"); ToggleVisibilityButton.Name = "ToggleVisibilityButton"; ToggleVisibilityButton.Size = UDim2.new(0, 80, 0, 30); ToggleVisibilityButton.Position = UDim2.new(0, 10, 0.5, 0); ToggleVisibilityButton.BackgroundColor3 = Color3.fromRGB(0, 0, 0); ToggleVisibilityButton.BackgroundTransparency = 0.4; ToggleVisibilityButton.Font = Enum.Font.SourceSansBold; ToggleVisibilityButton.Text = "Hide"; ToggleVisibilityButton.TextColor3 = Color3.fromRGB(255, 255, 255); ToggleVisibilityButton.TextSize = 14; ToggleVisibilityButton.Draggable = true; ToggleVisibilityButton.Active = true; ToggleVisibilityButton.Parent = ScreenGui
local UICorner_Toggle = Instance.new("UICorner"); UICorner_Toggle.CornerRadius = UDim.new(0, 8); UICorner_Toggle.Parent = ToggleVisibilityButton

local function UpdateSlider(value) local min, max = 16, 150; local percentage = (value - min) / (max - min); SliderHandle.Position = UDim2.new(percentage, -10, 0.5, -10); _G.WalkSpeed = value; SpeedSliderLabel.Text = "Speed: " .. math.floor(value) end
MediumSpeedButton.MouseButton1Click:Connect(function() UpdateSlider(60) end)
FastSpeedButton.MouseButton1Click:Connect(function() UpdateSlider(120) end)
ResetSpeedButton.MouseButton1Click:Connect(function() UpdateSlider(16) end)
local isDragging = false; SliderHandle.InputBegan:Connect(function(input) if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then isDragging = true end end); SliderHandle.InputEnded:Connect(function(input) if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then isDragging = false end end); UserInputService.InputChanged:Connect(function(input) if isDragging and (input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch) then local mouseX = input.Position.X; local startX = SliderBackground.AbsolutePosition.X; local width = SliderBackground.AbsoluteSize.X; local percentage = math.clamp((mouseX - startX) / width, 0, 1); local min, max = 16, 150; local newValue = min + (max - min) * percentage; UpdateSlider(newValue) end end)
ToggleVisibilityButton.MouseButton1Click:Connect(function() MainFrame.Visible = not MainFrame.Visible; ToggleVisibilityButton.Text = MainFrame.Visible and "Hide" or "Show" end)
AutoCatchButton.MouseButton1Click:Connect(function() _G.AutoCatchEnabled = not _G.AutoCatchEnabled; if _G.AutoCatchEnabled then AutoCatchButton.Text = "Auto Catch: ON"; AutoCatchButton.BackgroundColor3 = Color3.fromRGB(50, 200, 50) else AutoCatchButton.Text = "Auto Catch: OFF"; AutoCatchButton.BackgroundColor3 = Color3.fromRGB(200, 50, 50) end end)
TargetAnimalInput.FocusLost:Connect(function(enterPressed) if enterPressed then _G.TargetAnimalName = TargetAnimalInput.Text end end)
local fruitsToBuy = {"Carrot", "Apple", "Corn", "Pepper", "Banana", "Cherries"}; local BuyTreatEvent = nil
local function AttemptToBuyAllFruits() if not _G.AutoBuyTreats or not BuyTreatEvent then return end; for _, fruitName in ipairs(fruitsToBuy) do for i = 1, 10 do pcall(function() BuyTreatEvent:FireServer(fruitName) end); task.wait(0.05) end end end
AutoBuyButton.MouseButton1Click:Connect(function() _G.AutoBuyTreats = not _G.AutoBuyTreats; if _G.AutoBuyTreats then AutoBuyButton.Text = "Auto Buy: ON"; AutoBuyButton.BackgroundColor3 = Color3.fromRGB(50, 200, 50); AttemptToBuyAllFruits() else AutoBuyButton.Text = "Auto Buy: OFF"; AutoBuyButton.BackgroundColor3 = Color3.fromRGB(200, 50, 50) end end)
task.spawn(function() while task.wait(0.2) do pcall(function() if LocalPlayer.Character and LocalPlayer.Character:FindFirstChild("Humanoid") then LocalPlayer.Character.Humanoid.WalkSpeed = _G.WalkSpeed end end) end end)

LocalPlayer.CharacterAdded:Connect(function(character)
    task.wait(2)
    local humanoidRootPart = character:WaitForChild("HumanoidRootPart")
    if humanoidRootPart then
        currentPenLocation = humanoidRootPart.Position
    end
end)

local RemoteEvents = ReplicatedStorage:WaitForChild("Remote_Events", 10)
if RemoteEvents then
    local RestockEvent = RemoteEvents:WaitForChild("NOT", 5)
    BuyTreatEvent = RemoteEvents:WaitForChild("Buy_Treat", 5)
    local ThrowLassoEvent = RemoteEvents:WaitForChild("Throw", 5)
    if RestockEvent and BuyTreatEvent then
        RestockEvent.OnClientEvent:Connect(function(message)
            if message == "The treat shop has been restocked!" then
                AttemptToBuyAllFruits()
            end
        end)
    end
    if ThrowLassoEvent then
        task.spawn(function()
            while task.wait(0.5) do
                if _G.AutoCatchEnabled and _G.TargetAnimalName ~= "" then
                    pcall(function()
                        local targetModel = nil
                        for _, v in ipairs(getnilinstances()) do
                            if v.Name == _G.TargetAnimalName and v:IsA("Model") and v:FindFirstChild("HumanoidRootPart") then
                                targetModel = v
                                break
                            end
                        end
                        if targetModel then
                            local character = LocalPlayer.Character
                            local humanoidRootPart = character and character:FindFirstChild("HumanoidRootPart")
                            local head = character and character:FindFirstChild("Head")
                            if not (humanoidRootPart and head) then return end
                            keypress(Enum.KeyCode.One)
                            task.wait(0.2)
                            local startPos = humanoidRootPart.Position
                            local targetPos = targetModel.HumanoidRootPart.Position
                            local headPos = head.Position
                            ThrowLassoEvent:FireServer(LocalPlayer, startPos, targetPos, headPos)
                            task.wait(1.5)
                            if currentPenLocation then
                                humanoidRootPart.CFrame = CFrame.new(currentPenLocation)
                            end
                        end
                    end)
                end
            end
        end)
    end
end
